var alltitles;

function findingJson(title) {
  alltitles = title;
  // console.log(alltitles);
};

console.log(alltitle);
// $(document).ready(function(){
function like(hackathon) {

  $.ajax (
    url: "index.erb",
    type: "POST",
    data: {"hackathon": title, "points": '1'}
    success: function()
    {
      console.log(data);
    }
  )
}

//   $.post("/path", {parameter: x, parameter2:y}, function(){
//     $("#id-specifit to hackathon.id").html(changes the number to relect thedatabase);
//   })
//
//
// })
console.log("sanity checkz")

// Need to add ajax requests to database to pull number and disply to the website //


// Add votes to each profile //
